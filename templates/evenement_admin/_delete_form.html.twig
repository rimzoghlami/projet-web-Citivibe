<form method="post" action="{{ path('app_evenement_delete', {'ide': evenement.ide}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evenement.ide) }}">
    <button class="btn" style="background-color: transparant; border-radius: 30px; color: gray;">Supprimer</button>
</form>
 