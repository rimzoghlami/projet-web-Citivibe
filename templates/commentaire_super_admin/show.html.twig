{% extends 'base.html.twig' %}

{% block title %}Commentaire{% endblock %}

{% block body %}
    <h1>Commentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idc</th>
                <td>{{ commentaire.idc }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ commentaire.contenu }}</td>
            </tr>
            <tr>
                <th>Datecreation</th>
                <td>{{ commentaire.datecreation ? commentaire.datecreation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commentaire_index_superadmin') }}">back to list</a>

    <a href="{{ path('app_commentaire_edit_superadmin',  {'idc': commentaire.idc, 'ide': commentaire.evenementRelation.ide}) }}">edit</a>

    {{ include('commentaire_super_admin/_delete_form.html.twig') }}
{% endblock %}
